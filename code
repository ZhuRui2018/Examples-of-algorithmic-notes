#include<cstdio>
#include<cmath>
int main()
{
    double a,b,c;
    scanf("%lf%lf%lf",&a,&b,&c);
    double r1,r2,delta;
    delta=b*b-4*a*c;
    r1=(-b+sqrt(delta))/(2*a);
    r2=(-b-sqrt(delta))/(2*a);
    printf("r1=%7.2f\nr2=%7.2f",r1,r2);
    return 0;
}
